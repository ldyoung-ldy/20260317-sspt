# Step 3 报名流程验收清单

> 验收日期: 2026-03-21 | 状态: ✅ 核心功能已通过

## 1. 报名表单页 `/events/[slug]/register`

### 页面渲染
- [x] 已登录用户可正常打开
- [x] 未登录用户重定向到登录页
- [x] 赛事名称、报名窗口时间正确展示

### 自定义字段渲染
- [x] text 类型正确渲染
- [x] textarea 类型正确渲染
- [x] url 类型正确渲染
- [x] select 类型下拉正确渲染
- [x] required 字段未填时提示校验错误

### 提交逻辑
- [x] 提交成功后跳转到"我的报名"
- [x] 报名窗口外不展示表单，显示提示文案
- [x] 已报名用户再次访问显示"已报名"状态
- [x] 并发重复报名返回明确冲突提示（非通用错误）

### 字段身份匹配
- [x] 自定义字段使用 fieldId 匹配（已修复，非按索引匹配）
- [x] stale 表单提交被正确拦截

## 2. 我的报名页 `/my/registrations`

### 列表展示
- [x] 页面可正常打开
- [x] 显示已提交的报名记录
- [x] 每张卡片包含：赛事名称、报名时间、队伍名称、赛事时间
- [x] 报名状态 Badge 正确展示

### 状态操作
- [x] ACCEPTED 状态可点击"确认参赛"
- [x] ACCEPTED/CONFIRMED 状态可点击"取消报名"
- [x] PENDING/REJECTED/CANCELLED 不显示操作按钮（或仅显示合法操作）

### 空状态
- [x] 无报名记录时显示空状态提示
- [x] 空状态下有"去看赛事"引导按钮

## 3. 后台报名管理页 `/admin/events/[id]/registrations`

### 列表展示
- [x] 页面可正常打开
- [x] 显示该赛事所有报名记录
- [x] 表格列包含：用户、邮箱、队伍、状态、报名时间

### 状态计数
- [x] 顶部 MetricCard 显示各状态报名数量

### 筛选
- [x] 状态筛选可用
- [x] 关键字搜索可用
- [x] 重置按钮可清除筛选条件

### 批量操作
- [x] 可多选待审核报名
- [x] 批量接受按钮正常工作
- [x] 批量拒绝按钮正常工作
- [x] 非 PENDING 状态的报名不可被批量操作

### CSV 导出
- [x] 导出按钮正常下载 CSV
- [x] CSV 包含筛选后的数据
- [x] CSV 字段完整

## 4. 赛事详情页报名入口联动

### 报名状态卡片
- [x] 未登录用户：显示"请先登录"提示
- [x] 报名窗口内 + 未报名：显示"立即报名"按钮
- [x] 已报名用户：显示报名状态 Badge + "查看我的报名"链接
- [x] 报名窗口外：显示"报名已结束"或"报名未开启"文案
- [x] 管理员账号也可直接报名（显示提示文案）

## 5. Header 导航联动

- [x] 已登录用户可看到"我的报名"链接
- [x] 点击后跳转到 `/my/registrations`

## 6. 状态机逻辑

- [x] `registration-status.ts` 纯函数模块已抽取
- [x] `canTransition` / `canConfirmRegistration` / `canCancelRegistration` 已有单元测试
- [x] 非法状态转换被拦截

## 7. 代码审查修复项

- [x] **P1 修复**：自定义字段按 fieldId 匹配替代按索引匹配，避免字段调整后静默串值
- [x] **P1 修复**：并发重复报名（Prisma P2002）返回明确冲突提示，替代通用错误

## 已知风险 / 环境问题

- **Neon 间歇性连通抖动**：live QA 期间偶发 Prisma 查询超时，需要重试
- **历史样本一致性**：验收赛事存在 `registrationStart < startDate` 的历史数据，不影响新报名流程
- **测试数据清理策略**：验收数据保留于开发数据库，不影响功能正确性；后续可在部署前统一清理

## 8. 2026-03-21 登录态报名链路与页面回归 QA

### 本轮新增通过

- 通过导入 `localhost` 浏览器 cookie 拿到已登录会话，`/my/registrations` 可正常打开，空状态提示与“去看赛事”引导按钮展示正常
- 赛事详情页 `/events/[slug]` 在已登录状态下可正常打开，报名状态卡片、立即报名入口、赛事时间线与配置概览展示正常
- 报名表单页 `/events/[slug]/register` 在已登录状态下可正常打开，报名窗口信息、摘要区、补充信息字段与底部提交操作区展示正常
- 后台报名管理页 `/admin/events/[id]/registrations` 在管理员登录态下可正常打开，状态计数、筛选区、导出入口与空状态展示正常
- 上述页面均完成桌面 / 平板 / 手机视口检查，未发现阻塞性的布局问题
- 本轮检查到的报名相关页面控制台均无报错

### 本轮范围说明

- 本轮重点是登录态页面回归与响应式检查，不是“报名提交 -> 后台审核 -> 用户确认”的完整业务闭环验收
- 本轮覆盖的是 Step 3 已完成功能在真实登录会话下的页面可用性、入口联动与空状态 / 表单状态展示

### 本轮关注点

- 后台报名管理页与报名表单在手机宽度下仍偏紧凑，但当前不影响核心使用路径
